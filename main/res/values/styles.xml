<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
    </style>
    <!-- Custom style for highlighted dates in CalendarView -->
    <style name="CustomCalendarDate">
        <item name="android:background">@drawable/custom_calendar_date_background</item>
        <item name="android:textColor">@color/red</item>
    </style>

    <style name="CalendarStyle">
        <item name="android:weekDayTextAppearance">@style/WeekDayTextAppearance</item>
        <item name="android:dateTextAppearance">@style/DateTextAppearance</item>
    </style>

    <style name="WeekDayTextAppearance">
        <item name="android:textColor">@color/primary</item>
    </style>

    <style name="DateTextAppearance">
        <item name="android:textColor">@color/primaryDark</item>
    </style>
    <style name="CalendarHeaderTextAppearance" parent="TextAppearance.AppCompat.Large">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="CalendarDateTextAppearance" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textColor">@android:color/black</item>
    </style>

</resources>
